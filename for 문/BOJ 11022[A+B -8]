#include <stdio.h>


int main(void)
{ 
  int T, i, a, b;
  scanf("%d", &T);
  for (i = 1; i < T + 1; i++)
  { 
    scanf("%d %d", &a, &b);
    printf("Case #%d: %d + %d = %d\n", i, a, b, a + b);
  }
  return 0;
}
